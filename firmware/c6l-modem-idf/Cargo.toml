[package]
name = "c6l-modem-idf"
version = "0.1.0"
edition = "2021"

[dependencies]
esp-idf-sys = { version = "0.38", features = ["binstart"] }
esp-idf-hal = { version = "0.47", features = ["critical-section"] }
esp-println = { version = "0.13", features = ["esp32c6"] }
anyhow = "1"

[build-dependencies]
embuild = "0.33"

[workspace]
